"use client";

/**
 * Bottom-left presence chrome — live country bubble map.
 * Re-exports the bubble map under the historical PresenceStatus name
 * so chrome mounts stay stable.
 */

export { PresenceBubbleMap as PresenceStatus } from "@/components/presence-bubble-map";
